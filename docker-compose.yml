
services:
  resume-magic:
    build: .
    ports:
      - "3000:3000"
    environment:
      - BROWSERLESS_API_KEY=${BROWSERLESS_API_KEY}
      - BROWSERLESS_URL=${BROWSERLESS_URL}
      - BASE_URL=${BASE_URL}
    volumes:
      - .:/app
      - /app/node_modules
    restart: unless-stopped
